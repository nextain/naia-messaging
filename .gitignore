# Dependencies
node_modules/

# Runtime state and secrets — never tracked
.runtime/
.secrets/
.agents/session-contracts/.recovery/
.agents/harness/state/
.agents/harness/leases/
coverage/

# Instance config and secrets live in the instance, never here
*.local
*.local.*
.env
.env.*
!.env.example
!.env.sample
config/participants*.json
!config/participants.sample.json

# Editor / OS
*.swp
.DS_Store
