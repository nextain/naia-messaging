# naia-messaging — 에이전트 안내

이 저장소는 Naia 대화 계층의 코드다. 사람이 읽는 개요는 `README.md` 에 있다.
`AGENTS.md` 가 정본이고 `CLAUDE.md`·`GEMINI.md` 는 이 파일과 바이트 동일한
사본이며, `CODEX.md` 는 이 파일을 가리키는 짧은 포인터다.

## 무엇이 여기 있고 무엇이 없나

- 여기 있는 것: 전송 중립 `core`, `adapters/discord`, 호스트별 `runtime`, 검증
  스크립트. 전부 일반 엔진·어댑터 코드다.
- 여기 없는 것(그리고 절대 넣지 않는 것): 봇 토큰과 모든 비밀, 실제 guild·channel·
  thread 식별자, 실제 호스트 이름·IP, 참가자 명부, 인스턴스별 감시 설정. 이런 것은
  인스턴스(개인 ADK·팀 ADK)의 설정으로 남는다.

## 층 경계

- `core/` 는 어떤 전송 수단도 몰라야 한다. Discord 라는 단어, snowflake, 멘션 배열
  같은 것이 `core/` 에 들어오면 잘못된 것이다.
- `adapters/<transport>/` 만 그 전송 수단의 사정을 안다. 새 전송 수단은 core 계약을
  그대로 구현하는 어댑터를 더하는 일이지 core 를 고치는 일이 아니다.
- `runtime/` 은 인스턴스 설정의 **형태**만 검증한다. 실제 값을 담지 않는다.

## 작업 규칙

- 비밀은 값이 아니라 그것을 담은 환경 변수의 이름으로만 참조한다.
- 사람에게 확인을 부탁할 때는 네 가지(누가·무엇을·정확히 어디서·어디로 보고)를 적는다.
- 게시 성공을 수신으로, 수신을 시작으로 착각하지 않는다. 수신 확인은 시작 영수증이다.
- 커밋 전 `npm test` 를 돌린다. 공개 안전성 검사가 포함되며, 실패하면 push 하지 않는다.
- 공개 저장소 생성·전환, 비공개 내용의 공개 push 는 소유자 승인 SHA 와 전체 검수 뒤에만.

## 검증

```
npm test        # 단위 테스트 + 공개 안전성 검사
```
